# Disk Analyzer (Вариант 4)

Консольная программа для проверки свободного места на дисках.

## ⚠️ ВАЖНО: Требования перед запуском
Для работы **обязательно** нужна **Java 17** или **Java 21**.
**Не используйте Java 24 (Beta)** — с ней программа не соберется.

Проверить свою версию:
```bash
java -version
```

## 🚀 Пошаговая инструкция по запуску


### 1. Собрать проект
Эта команда сама скачает нужные библиотеки и создаст файл программы.
Windows:
```Powershell
./gradlew build
```

Linux / macOS:
```Bash
chmod +x gradlew
./gradlew build
```

Дождитесь сообщения BUILD SUCCESSFUL.

### 2. Запустить

```Bash
java -jar build/libs/disk-analyzer-1.0.0.jar
```

## ⚙️ Настройки

Если нужно изменить порог срабатывания (по умолчанию 90%), отредактируйте файл:
src/main/resources/app.properties

```Properties
usage.threshold.percent=90
output.units=GB
```

После изменения файла нужно снова выполнить команду ./gradlew build.
